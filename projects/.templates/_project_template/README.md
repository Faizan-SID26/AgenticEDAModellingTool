# &lt;project name&gt;

Created from `_project_template`. Use the slash commands from Claude Code:

- `/init` — profile data files in `data/` and produce `memory/INIT_PROFILE.json`
- `/plan` — adaptive Q&A → `MISSION.json` and friends
- `/run` — autonomous bootstrap + iteration + finalize
- `/contribute` — open a PR that merges learnings into `knowledge/`
